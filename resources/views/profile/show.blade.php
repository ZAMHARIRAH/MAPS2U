@extends('layouts.app', ['title' => 'View Profile'])
@section('content')
<div class="profile-card"><div class="profile-top"><img class="profile-avatar" src="{{ $user->profilePhotoUrl() }}" alt="Profile picture"><div><h1>{{ $user->name }}</h1><p>{{ $user->roleLabel() }}</p></div></div><div class="details-grid"><div><span>Name</span><strong>{{ $user->name }}</strong></div><div><span>Email Address</span><strong>{{ $user->email }}</strong></div><div><span>Phone Number</span><strong>{{ $user->phone_number ?: '-' }}</strong></div><div><span>Role</span><strong>{{ $user->roleLabel() }}</strong></div>@unless($user->isTechnician())<div class="full"><span>Address</span><strong>{{ $user->address ?: '-' }}</strong></div>@endunless</div><div class="action-row"><a class="btn primary" href="{{ route('profile.edit') }}">Edit Account</a>@if($user->isClient())<form method="POST" action="{{ route('profile.destroy') }}" data-delete-confirm="Are you sure you want to delete your account?">@csrf @method('DELETE')<button class="btn danger" type="submit">Delete Account</button></form>@endif</div></div>
@endsection
