@extends('layouts.app', ['title' => 'SSU Account'])
@section('content')
<div class="profile-card"><div class="profile-top"><img class="profile-avatar" src="{{ $ssu->profilePhotoUrl() }}" alt="Profile picture"><div><h1>{{ $ssu->name }}</h1><p>{{ $ssu->roleLabel() }}</p></div></div><div class="details-grid"><div><span>SSU Name</span><strong>{{ $ssu->name }}</strong></div><div><span>Email</span><strong>{{ $ssu->email }}</strong></div><div><span>Phone Number</span><strong>{{ $ssu->phone_number ?: '-' }}</strong></div><div><span>Role</span><strong>{{ $ssu->roleLabel() }}</strong></div><div class="full"><span>Region States</span><strong>{{ collect($ssu->region_states ?? [])->join(', ') ?: '-' }}</strong></div><div class="full"><span>Address</span><strong>{{ $ssu->address ?: '-' }}</strong></div></div><div class="action-row"><a class="btn ghost" href="{{ route('admin.ssu.index') }}">Back</a><a class="btn primary" href="{{ route('admin.ssu.edit', $ssu) }}">Edit</a><form method="POST" action="{{ route('admin.ssu.destroy', $ssu) }}" onsubmit="return confirm('Delete this SSU account?');">@csrf @method('DELETE') <button class="btn danger" type="submit">Delete</button></form></div></div>
@endsection
