@extends('layouts.app', ['title' => 'Edit SSU'])
@section('content')
<div class="auth-card wide"><h1>Edit SSU Account</h1><form method="POST" action="{{ route('admin.ssu.update', $ssu) }}">@csrf @method('PUT') <label>SSU Name</label><input type="text" name="name" value="{{ old('name', $ssu->name) }}" required><label>SSU Email Address</label><input type="email" name="email" value="{{ old('email', $ssu->email) }}" required><label>SSU Phone Number</label><input type="text" name="phone_number" value="{{ old('phone_number', $ssu->phone_number) }}" required><label>SSU Region</label><div class="checkbox-chip-grid">@foreach($stateOptions as $state)<label class="inline-check"><input type="checkbox" name="region_states[]" value="{{ $state }}" {{ in_array($state, old('region_states', $ssu->region_states ?? [])) ? 'checked' : '' }}> {{ $state }}</label>@endforeach</div><label>New Password <small>(leave blank if no change)</small></label><input type="password" name="password"><label>Confirm New Password</label><input type="password" name="password_confirmation"><div class="action-row"><button class="btn primary" type="submit">Update Account</button><a class="btn ghost" href="{{ route('admin.ssu.index') }}">Cancel</a></div></form></div>
@endsection
